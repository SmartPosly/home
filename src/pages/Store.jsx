import React from "react";

const Store = () => (
  <div style={{ display: "flex", justifyContent: "center", alignItems: "center", height: "100vh", fontSize: "3rem", fontWeight: "bold", color: "#1f2937" }}>
    قريباً
  </div>
);

export default Store; 